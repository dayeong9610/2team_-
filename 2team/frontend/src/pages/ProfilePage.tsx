// =====================================================================
// ProfilePage (내 프로필/기록) — 라우트: "/profile"
// ---------------------------------------------------------------------
// 아직 미구현 상태의 자리 표시(placeholder) 화면입니다. 제목과 안내
// 문구 한 줄만 있고, 실제 콘텐츠/로직은 없습니다.
//
// [Backend 연동 참고]
// 현재 Backend API 호출이 전혀 없습니다. 이 서비스는 회원가입/로그인이
// 없는 구조라서, 사용자별 기록을 영구 저장·조회하는 기능 자체가
// 아직 설계되지 않았어요. 나중에 이 화면을 실제로 채우게 되면,
// 어떤 API(예: 사용자별 GET /api/.../history 같은)가 필요할지
// 프론트-백엔드가 같이 논의해야 합니다.
// =====================================================================

function ProfilePage() {
  return (
  <main className="page-container">

    <h1>나의 대응능력</h1>

    <p>나의 학습 결과를 확인하는 화면입니다.</p>
  </main>
  );
}

export default ProfilePage;
