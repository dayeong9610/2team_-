# ============================================================
# 마냥이 Backend - direct dependencies only
# Python 가상환경(.venv)에서 설치:
#   python -m pip install -r requirements.txt
#
# 기존 pip freeze의 모든 transitive package를 저장하지 않고,
# 현재 backend/src와 backend/tests에서 직접 사용하는 패키지만 정리했습니다.
# ============================================================

# Web / API
fastapi==0.141.1
uvicorn==0.52.4
jinja2==3.1.6
python-multipart==0.0.32

# Database
sqlmodel==0.0.42
SQLAlchemy==2.0.52
psycopg2-binary==2.9.13

# Settings / validation
pydantic==2.13.5
python-dotenv==1.2.3

# Admin authentication
python-jose==3.5.0
passlib==1.7.4
bcrypt==4.0.1

# AI / LLM
langchain-core==1.6.1
langchain-openai==1.6.0
langchain-google-genai==4.4.0

# Test
httpx==0.28.1
pytest==9.1.1
pytest-asyncio==1.4.0

# QR코드 생성
qrcode[pil]>=8.0